root:                   C:\Users\user.name\_development\cygnet_to_ignition

to start up the venv:
    directory:          root 
    command:            .venv/Scripts/activate 

add library using uv:
    directory:          root
    command:            uv add [insert library name]

git save workflow: 
    directory:          root
    commands:           git status
                        git add *
                        git commit -m "End of day: <describe changes>"
                        git pull origin main    # skip if working alone
                        git push origin main

git latest changes:
    directory:          root
    command:            git pull origin main    # sync with remote